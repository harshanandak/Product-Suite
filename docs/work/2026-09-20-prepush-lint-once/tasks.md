# Tasks

Issue: `e54d032b-92f6-4500-adf8-5e59ffba57d2`.

1. Sol implementer: read the six scoped files and callers; add behavior regressions and execute the failing cases before production edits.
2. Sol implementer: move lint ownership into the gate, reuse command descriptors and remove duplicate local lint steps without changing canonical CI commands; run focused GREEN and repo-tooling checks.
3. Lead: obtain sequential spec and quality/security reviews, resolve in-scope findings, run required validation, commit/push through the personal account, and prepare a merge-ready PR with exact-head evidence.

Acceptance and exclusions are in `plan.md`. Do not expand into the broader affected-only or DB optimization issues.
