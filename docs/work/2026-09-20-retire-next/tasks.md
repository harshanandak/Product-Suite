# Tasks

1. Sol implementer owns the retirement graph, archive marker, lock and focused regression changes; read all touched files, run the smallest meaningful failing graph test before production edits, then run focused checks and frozen-install/audit proof.
2. Lead owns delivery settings, changelog, this plan and issue state; prepare the required-context transition with a reverse path and supported coverage evidence.
3. Independent reviewer checks the full retirement diff and historical migration preservation. Lead integrates findings, runs required validation and normal guarded push, opens the separate PR, verifies exact-head checks and hands off for human merge.
