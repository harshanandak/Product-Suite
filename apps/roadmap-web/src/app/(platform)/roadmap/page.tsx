import { PlatformModuleCard } from "@/components/platform/platform-module-card";

export default function RoadmapPage() {
  return (
    <PlatformModuleCard
      title="Roadmap module"
      description="Continue roadmap planning from the Product Suite shell while existing workspace URLs remain available."
    />
  );
}
