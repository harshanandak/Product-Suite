import { PlatformModuleCard } from "@/components/platform/platform-module-card";

export default function AgentsPage() {
  return (
    <PlatformModuleCard
      title="Agents module"
      description="Reserved shell entry for automation agents, runs, and review queues."
    />
  );
}
