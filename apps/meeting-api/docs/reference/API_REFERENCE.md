# API Reference

Hosted authentication and onboarding endpoints:

- Neon Auth session exchange: `/api/auth/session/exchange`
- Hosted onboarding invitations: `/api/auth/onboarding/invitations`
- Browser redirect callback: `/auth/callback`

The hosted auth model uses app-owned organizations and removes the legacy provider-specific auth route family.
