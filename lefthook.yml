pre-commit:
  jobs:
    - name: source-test coupling
      run: bun run check:source-test

pre-push:
  jobs:
    - name: pre-push gate
      run: node scripts/prepush-gate.mjs --pre-push
      use_stdin: true
