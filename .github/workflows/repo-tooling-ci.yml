name: Repo Tooling CI

on:
  push:
    branches:
      - main
    paths:
      - "README.md"
      - "CHANGELOG.md"
      - "docs/**"
      - "test/**"
      - "scripts/**"
      - "package.json"
      - "bun.lock"
      - ".github/workflows/repo-tooling-ci.yml"
  pull_request:
    paths:
      - "README.md"
      - "CHANGELOG.md"
      - "docs/**"
      - "test/**"
      - "scripts/**"
      - "package.json"
      - "bun.lock"
      - ".github/workflows/repo-tooling-ci.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  repo-tooling:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run root tooling tests
        run: bun test test/repo-tooling.test.js test/domain-inventory.test.js
