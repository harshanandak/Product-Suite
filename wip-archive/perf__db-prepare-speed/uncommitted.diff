diff --git a/docs/work/2026-09-19-db-prepare-speed/decisions.md b/docs/work/2026-09-19-db-prepare-speed/decisions.md
index 4f3edab..3897ef8 100644
--- a/docs/work/2026-09-19-db-prepare-speed/decisions.md
+++ b/docs/work/2026-09-19-db-prepare-speed/decisions.md
@@ -1,3 +1,11 @@
 # DB Contract redundant prepare reduction - decisions
 
 No implementation-time specification gaps recorded.
+
+## 2026-09-21 - confirmed delivery target and resumed scope
+
+The user confirmed a maximum of 15 minutes for the whole DB workflow, not only the test suite, then requested continuation and merged prerequisite PR #191. The parent Kernel acceptance is authoritative; there is no eight-minute minimum.
+
+Carry only optimization commit `e8cb748` onto merge `575afded`, preserving the already-merged PR #187 lease hardening. Recovery remains available at `archive/db-prepare-before-191-20260921`.
+
+Source and retained telemetry indicate seven-to-five alone is unlikely to meet the whole-workflow budget. Child issue `59c1ab1c-d0aa-41f6-b964-63c68da6d11d` adds existing phase measurement to dedicated branches. Run one diagnostic workflow before three-run acceptance. Preserve all assertions, isolation, cleanup and the two-branch cap. Template/fork redesign remains outside this implementation slice.
diff --git a/docs/work/2026-09-19-db-prepare-speed/plan.md b/docs/work/2026-09-19-db-prepare-speed/plan.md
index 6b1971f..4eb5cf2 100644
--- a/docs/work/2026-09-19-db-prepare-speed/plan.md
+++ b/docs/work/2026-09-19-db-prepare-speed/plan.md
@@ -2,7 +2,7 @@
 
 ## Outcome
 
-Reduce the required DB Contract lane from seven transactional branch prepare lifecycles to five while preserving its 9-file, 57-test, zero-skip evidence contract.
+Reduce the required DB Contract lane from seven transactional branch prepare lifecycles to five while preserving its 9-file, 57-test, zero-skip evidence contract. The user-confirmed delivery target is at most 15 minutes for the whole workflow, including prerequisites, setup, tests, cleanup, inter-job waits and the final required verdict.
 
 ## Design
 
@@ -12,20 +12,24 @@ Move each isolated transactional tail assertion into the file's existing transac
 
 Add one AST-backed topology regression that requires every routed file with transactional assertions to instantiate exactly one transactional suite factory. The existing topology test continues to prove transactional and dedicated lifecycle scopes do not overlap.
 
+Instrument the dedicated branch lifecycle with the existing phase telemetry, under child issue `59c1ab1c-d0aa-41f6-b964-63c68da6d11d`. Dedicated create/prepare/delete work is currently omitted from phase totals. Preserve execution order, error propagation, strict deletion proof and lease release. Add focused success and failure regressions using the existing test framework. Measurements must not expose credentials or connection strings.
+
 ## Constraints
 
 - Keep all 57 assertions and their titles unchanged; no skips or timeout increases.
 - Preserve dedicated branches for concurrency, committed retry/redrive, migration history, and least-privilege behavior.
 - Keep independent sessions, rollback sentinels, branch cap, cleanup/deletion proof, rate-limit behavior, workflow gating, schema, auth, and product behavior unchanged.
 - Add no dependency or framework and do not share a mutable test database across files.
-- Limit the change to the two mixed DB Contract suites, the structural topology regression, and these work documents.
+- Limit changes to the two mixed suites, topology regression, dedicated lifecycle telemetry and its focused tests, changelog and these work documents. No template/fork redesign is authorized by this slice.
 
 ## Verification
 
 1. RED: run the focused topology test and observe the new single-factory invariant fail for `accept-path` and `meeting-ingest`.
 2. GREEN: reorder the two transactional tails, remove their extra factories, and rerun the focused topology and suite-resource unit tests locally.
 3. Self-review against `CODING_STANDARDS.md`; inspect the final diff and count-lock evidence.
-4. Real Neon acceptance is deferred to the lead: three consecutive runs on one final SHA, each 8-15 minutes, 9 files / 57 tests / 0 skips, no current-run branches remaining, and at most two active branches.
+4. For dedicated lifecycle instrumentation, first demonstrate a failing focused regression for missing phase records, then passing success/failure coverage with unchanged cleanup and error semantics.
+5. The lead runs one diagnostic workflow before performance acceptance. Seven-to-five alone has no measured saving and may not meet the target; do not spend three acceptance runs on a known over-budget candidate.
+6. Acceptance requires three consecutive whole DB workflows on one final SHA, each at most 15 minutes from workflow start through the final required verdict. Retain 9 files / 57 tests / 0 skips and any extra refreshed-base coverage, no current-run branches remaining, at most two active ephemeral branches and stable rate limits. Faster than eight minutes passes; report component timings separately.
 
 ## Reverse path
 
diff --git a/docs/work/2026-09-19-db-prepare-speed/tasks.md b/docs/work/2026-09-19-db-prepare-speed/tasks.md
index 9d02819..2a64199 100644
--- a/docs/work/2026-09-19-db-prepare-speed/tasks.md
+++ b/docs/work/2026-09-19-db-prepare-speed/tasks.md
@@ -17,3 +17,15 @@
 1. Read `CODING_STANDARDS.md`, inspect the final diff, and verify all 57 manifest entries remain unchanged.
 2. Commit the focused change without pushing.
 3. Return RED/GREEN evidence and the exact commit to the lead for independent spec and quality review.
+
+## Task 4 - measure dedicated branch lifecycle cost
+
+OWNS: `apps/platform-api/test/db-contract/harness.ts`, the focused dedicated lifecycle test in `apps/platform-api/test/db-contract/telemetry.test.ts` or one narrowly scoped test file in that directory, and this task's work documents/changelog entry.
+
+1. Trace all `withDedicatedDbBranch` callers and the existing `measurePhase` helper before editing.
+2. Write and run a focused failing regression proving dedicated lifecycle create/prepare/delete records are missing. Cover successful execution and failure cleanup without live database access.
+3. Reuse existing phase telemetry around the dedicated lifecycle without changing branch allocation, preparation, seeding, body execution, deletion verification or lease release semantics. Avoid new abstractions and dependencies.
+4. Run the regression to green and the relevant existing telemetry/resource/topology tests. Prove original errors are preserved and cleanup failures are not masked; never emit credentials or connection strings.
+5. Commit with normal hooks, then return exact diff and test evidence for spec review followed by quality review. The lead owns pushing and live workflow execution.
+
+Task 4 supports measurement; it is not a claim that the whole-workflow timing target has been met. The lead first runs one diagnostic workflow, then starts three-run acceptance only when the candidate meets the budget.
