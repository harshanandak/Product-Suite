{
  "name": "@product-suite/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:catalog-rollback": "vitest run test/catalog-rollback.test.ts",
    "test:sql-firewall": "vitest run test/sql-firewall.test.ts",
    "lint": "eslint --max-warnings 0 src drizzle.config.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.28.0",
    "eslint": "9.39.2",
    "typescript": "^5.6.0",
    "vitest": "^4.0.0"
  }
}
